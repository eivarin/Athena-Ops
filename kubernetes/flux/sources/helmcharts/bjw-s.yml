apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: app-template
  namespace: flux-system
spec:
  interval: 24h
  chart: app-template
  version: 3.6.0
  sourceRef:
    kind: HelmRepository
    name: bjw-s
    namespace: flux-system
