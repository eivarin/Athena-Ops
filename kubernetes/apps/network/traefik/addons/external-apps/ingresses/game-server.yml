apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: game-server
spec:
  parentRefs:
    - name: traefik-private-default
      port: 443
  hostnames:
    - "game-server.${SECRET_PRIVATE_DOMAIN}"
  rules:
    - backendRefs:
        - name: game-server
          port: 80