---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name trek-secrets
  namespace: default
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: *name

  dataFrom:
    - find:
        name:
          regexp: &prefix "TREK_(.*)"
      rewrite:
        - regexp:
            source: *prefix
            target: "$1"
